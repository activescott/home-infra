# see Filter Rules at https://linux.die.net/man/1/rsync 
# Essentially we copy everything EXCEPT what is excluded

# dumb finder:
.DS_Store

# files we use in git for scripting, but not in prod:
rsync-excluded-files
deploy-from-git-to-prod.sh
diff-git-and-prod.sh

# directories that we don't maintain in git (generated and maintained by app):
data/
var-lib-mysql/
storage/
